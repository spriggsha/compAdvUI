import { Given,When,Then } from 'cucumber';
import checkTitle from "../support/check/checkTitle";
import isVisible from "../support/check/isDisplayed";
import new_entity_page from "../page_objects/newEntityPage";
import checkEqualsText from "../support/check/checkEqualsText";

// Then(
//     /^I expect that the title is( not)* "([^"]*)?"$/,
//     checkTitle
// );



// Then(
//     /^I expect the Full Name label to be displayed$/,
//     isVisible(new_entity_page.nameLabel)
// );
//
//
// Then(
//     /^I expect the Full Name label to contain the text "(.*)"$/,
//     checkEqualsText(element,new_entity_page.nameLabel)
// );
//
//
// Then(
//     /^I expect the Full Name input field to be displayed$/,
//     isVisible(new_entity_page.nameInput)
// );
//
// Then(
//     /^I expect the Country label to be displayed$/,
//     isVisible(new_entity_page.countryLabel)
// );
//
// Then(
//     /^I expect the Country input field to be displayed$/,
//     isVisible(new_entity_page.countryInput)
// );
//
// Then(
//     /^I expect the Dob label to be displayed$/,
//     isVisible(new_entity_page.dobLabel)
// );
//
// Then(
//     /^I expect the Dob input field to be displayed$/,
//     isVisible(new_entity_page.dobInput)
// );
// Then(
//     /^I expect the Position label to be displayed$/,
//     isVisible(new_entity_page.positionLabel)
// );
// Then(
//     /^I expect the Position input field to be displayed$/,
//     isVisible(new_entity_page.positionInput)
// );
//
// Then(
//     /^I expect the Source Info URL label to be displayed$/,
//     isVisible(new_entity_page.sourceUrlLabel)
// );
//
// Then(
//     /^I expect the Source Info URL input field to be displayed$/,
//     isVisible(new_entity_page.sourceUrlInput)
// );
//
// Then(
//     /^I expect the Risk Level label to be displayed$/,
//     isVisible(new_entity_page.riskLevelLabel)
// );
//
// Then(
//     /^I expect the Risk Level dropdown field to be displayed$/,
//     isVisible(new_entity_page.riskLevelDropDown)
// );

//
//
//
// Then(
//     /^I expect the Dob label to be displayed$/,
//     isVisible(new_entity_page.dobLabel)
// );
// Then(
//     /^I expect the Dob label to be displayed$/,
//     isVisible(new_entity_page.dobLabel)
// );
// Then(
//     /^I expect the Dob label to be displayed$/,
//     isVisible(new_entity_page.dobLabel)
// );
// Then(
//     /^I expect the Dob label to be displayed$/,
//     isVisible(new_entity_page.dobLabel)
// );